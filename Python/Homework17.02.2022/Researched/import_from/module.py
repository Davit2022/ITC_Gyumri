#To use the functions, we need to write its names
#Fromm is designed to take a specific function from a module
#After that there is no need to write the name of the module
#we can also use as:
from my_module import hi, add as a

import my_module

hi ()
print (a (45, 15))
